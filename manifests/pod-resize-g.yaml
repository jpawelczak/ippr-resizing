apiVersion: v1
kind: Pod
metadata:
  name: resize-demo-g
spec:
  containers:
  - name: demo-g
    image: nginx:latest
    resources:
      requests:
        cpu: "1.0"
        memory: 1G
      limits:
        cpu: "1.0"
        memory: 1G